<ui:UiPage
    x:Class="Wpf.Ui.Gallery.Views.Pages.DialogsAndFlyouts.DialogPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Wpf.Ui.Gallery.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Wpf.Ui.Gallery.Views.Pages.DialogsAndFlyouts"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:system="clr-namespace:System;assembly=System.Runtime"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="DialogPage"
    d:DataContext="{d:DesignInstance local:DialogPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <ui:UiPage.Resources>
        <system:String x:Key="PageXamlUrl">https://github.com/lepoco/wpfui/blob/development/src/Wpf.Ui/Styles/Controls/Dialog.xaml</system:String>
        <system:String x:Key="PageCsharpUrl">https://github.com/lepoco/wpfui/blob/development/src/Wpf.Ui/Controls/Dialog.cs</system:String>
    </ui:UiPage.Resources>

    <Grid>
        <ui:DynamicScrollViewer
            x:Name="PageScrollViewer"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="280" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0" Margin="42">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <controls:ControlPresenterUserControl
                        Grid.Row="0"
                        Margin="0"
                        HeaderText="Global WPF UI Dialog above NavigationView control.">
                        <controls:ControlPresenterUserControl.ControlContent>
                            <Button
                                Command="{Binding ViewModel.ShowDialogCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:DialogPage}, Mode=OneWay}"
                                CommandParameter="{Binding RelativeSource={RelativeSource Self}, Mode=OneWay}"
                                Content="Show Dialog" />
                        </controls:ControlPresenterUserControl.ControlContent>
                        <controls:ControlPresenterUserControl.CodeText>
                            &lt;ui:Dialog Content=&quot;Hello&quot; /&gt;
                        </controls:ControlPresenterUserControl.CodeText>
                    </controls:ControlPresenterUserControl>
                </Grid>
            </Grid>
        </ui:DynamicScrollViewer>
        <controls:ControlSummaryUserControl CsharpUrl="{StaticResource PageCsharpUrl}" XamlUrl="{StaticResource PageXamlUrl}" />
    </Grid>
</ui:UiPage>
