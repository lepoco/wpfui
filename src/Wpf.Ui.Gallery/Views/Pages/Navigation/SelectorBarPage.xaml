<Page
    x:Class="Wpf.Ui.Gallery.Views.Pages.Navigation.SelectorBarPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Wpf.Ui.Gallery.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Wpf.Ui.Gallery.Views.Pages.Navigation"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="SelectorBarPage"
    d:DataContext="{d:DesignInstance local:SelectorBarPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <Grid Margin="0,0,0,24">
        <controls:ControlExample
            Margin="0"
            HeaderText="Standard SelectorBar.">
            <controls:ControlExample.XamlCode>
                &lt;ui:SelectorBar&gt;\n
                \t&lt;ui:SelectorBarItem Text=&quot;Recent&quot; Icon=&quot;{ui:SymbolIcon Clock24}&quot; /&gt;\n
                \t&lt;ui:SelectorBarItem Text=&quot;Shared&quot; Icon=&quot;{ui:SymbolIcon Share24}&quot; /&gt;\n
                \t&lt;ui:SelectorBarItem Text=&quot;Favorites&quot; Icon=&quot;{ui:SymbolIcon Star24}&quot; /&gt;\n
                &lt;/ui:SelectorBar&gt;
            </controls:ControlExample.XamlCode>
            <StackPanel>
                <ui:SelectorBar SelectedIndex="{Binding ViewModel.SelectedIndex, Mode=TwoWay}">
                    <ui:SelectorBarItem Text="Recent" Icon="{ui:SymbolIcon Clock24}" />
                    <ui:SelectorBarItem Text="Shared" Icon="{ui:SymbolIcon Share24}" />
                    <ui:SelectorBarItem Text="Favorites" Icon="{ui:SymbolIcon Star24}" />
                </ui:SelectorBar>
                <Border
                    Margin="0,12,0,0"
                    Padding="0,12,0,0"
                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                    BorderThickness="0,1,0,0">
                    <ui:TextBlock
                        FontTypography="Body"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="{Binding ViewModel.SelectedContent}" />
                </Border>
            </StackPanel>
        </controls:ControlExample>
    </Grid>
</Page>
